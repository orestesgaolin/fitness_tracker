// ignore_for_file: prefer_const_constructors
import 'package:steps_client/steps_client.dart';
import 'package:test/test.dart';

void main() {
  group('StepsClient', () {
    test('can be instantiated', () {
      expect(StepsClient(), isNotNull);
    });
  });
}
